spring:
  application:
    name: awesome-user-service
  cloud:
    config:
      discovery:
        enabled: true
        service-id: awesome-config-service
    
server:
  port: 8182
  
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8181/eureka/
